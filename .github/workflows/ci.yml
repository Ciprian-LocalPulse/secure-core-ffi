name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-native:
    name: Build & test (native)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.toml') }}

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Build (release)
        run: cargo build --release --verbose

      - name: Run unit + integration tests
        run: cargo test --release --verbose

      - name: Test Node.js binding
        working-directory: bindings/nodejs
        run: |
          npm install
          SECURITY_CORE_LIB=${{ github.workspace }}/target/release/libsecurity_core.so npm test

      - name: Test Python ctypes binding
        run: |
          PYTHONPATH=bindings/python SECURITY_CORE_LIB=${{ github.workspace }}/target/release/libsecurity_core.so python -m unittest discover -s bindings/python -p 'test_*.py'

      - name: Check native PyO3 binding
        run: cargo check --manifest-path bindings/python-native/Cargo.toml --locked

  build-wasm:
    name: Build WebAssembly module
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Build wasm target
        run: wasm-pack build --target web --out-dir bindings/wasm-nextjs/pkg
